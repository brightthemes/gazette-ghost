{{!-- Tag cloud --}}
<div class="tag-cloud lazyload animated fadeIn">
    {{#get "tags" limit="all" as |tag|}}
        {{#foreach tag}}
            <a href="{{url}}">{{name}}</a>
        {{/foreach}}
    {{/get}}
    <div class="clearfix"></div>
</div>
